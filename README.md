# GoodBooks API

A FastAPI + MongoDB project for managing books and ratings, containerized with Docker.

## 🚀 Setup

1. Clone the repo:
   ```bash
   git clone https://github.com/<your-username>/<your-repo-name>.git
   cd <your-repo-name>
